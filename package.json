{
  "name": "crash-game",
  "private": true,
  "workspaces": ["services/*", "packages/*", "frontend"],
  "scripts": {
    "docker:up": "docker compose up --build -d",
    "docker:down": "docker compose down",
    "docker:prune": "docker compose down -v --rmi all --remove-orphans && docker volume prune -f",
    "install:all": "bun install && cd services/games && bun run prisma:generate && cd ../wallets && bun run prisma:generate"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
